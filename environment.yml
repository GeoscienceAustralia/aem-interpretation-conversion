name: aemworkflow-env
channels:
  - conda-forge
  - defaults
dependencies:
  - gdal=3.10.2
  - pip=25.0.1
  - python=3.12.9
  - pip:
      # - git+https://github.com/GeoscienceAustralia/aem-interpretation-conversion.git@main
      - git+https://github.com/GeoscienceAustralia/aem-interpretation-conversion.git@PV-2480-add-flags-for-aem-workflow-cli-scripts#egg=aemworkflow

prefix: /opt/conda-env
