name: aemworkflow-env
channels:
  - conda-forge
  - defaults
dependencies:
  - gdal=3.10.2
  - pip=25.0.1
  - python=3.12.9
  - pip:
      - git+https://github.com/GeoscienceAustralia/aem-interpretation-conversion.git@PV-2471-align-anaconda-and-venv-setup-for-cli#egg=aemworkflow
prefix: /opt/conda-env
